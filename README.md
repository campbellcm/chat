# Chat with Colin

Responsive static landing page with iClosed booking and Unicorn Studio visuals.

Open `index.html`, or serve this folder with any static web server. No build step is required. The portrait is embedded in the HTML; booking and animations require an internet connection.
